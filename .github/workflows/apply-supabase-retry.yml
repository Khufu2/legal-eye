name: Apply Supabase retry fix

on:
  push:
    branches: [main]
    paths:
      - scripts/patch_supabase_retry.py
      - .github/workflows/apply-supabase-retry.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: python scripts/patch_supabase_retry.py
      - run: python -m py_compile services/docling/worker.py
      - run: |
          rm -f scripts/patch_supabase_retry.py .github/workflows/apply-supabase-retry.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Retry transient Supabase mutations"
          git push
