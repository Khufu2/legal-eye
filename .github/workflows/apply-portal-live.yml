name: Apply Portal live
on:
  push:
    paths:
      - scripts/patch_portal_live.py
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python scripts/patch_portal_live.py
      - run: |
          rm scripts/patch_portal_live.py .github/workflows/apply-portal-live.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/page.tsx app/refinement.css scripts/patch_portal_live.py .github/workflows/apply-portal-live.yml
          git commit -m "Activate secure client Portal"
          git push
